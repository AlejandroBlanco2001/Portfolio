export type buttonProps = {
    text: string;
    onClickFunction: (event: any) => void;
};

export type WorkShowcaseProps = {
    title: string;
    description: string;
    image: string;
    orientation: string;
    link: string;
};
